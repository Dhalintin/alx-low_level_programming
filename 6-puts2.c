#include "main.h"

/** This function takes a ponter n
* @*n
*and Updates the value to 98 
*/

void puts2(char *str)
{
    int len = strlen(s);
    for (int i = 0; i < len; i += 2) {
        printf("%c", s[i]);
    }
    printf("\n");

}