#include "main.h"
#include <stdlib.h>

/**
* malloc_unchecked: is a function that takes an integer 
* @b: integer value and
* Return: pointer 
*/

void *malloc_checked(unsigned int b)
{
int *c
c = malloc(b)
if(c == null)
exit(98);

return (c)
}
